[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev --no-engine-strict"
  SECRETS_SCAN_ENABLED = "false"

# Netlify Blobs configuration
[blobs]
  deploy = true

# Headers for World App webview iframe compatibility & PWA
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "ALLOWALL"
    Content-Security-Policy = "frame-ancestors *;"
    Access-Control-Allow-Origin = "*"
